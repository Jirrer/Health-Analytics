CREATE TABLE counties(name text not null, year integer, median_income integer, health_rank integer, Gini_Coeffient real, Deaths integer, Births integer);
CREATE TABLE info (calculation text, content text);
